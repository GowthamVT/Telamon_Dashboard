{
  "$comment": "Fixture descriptor used by api.test.js. Mirrors what introspect-schema.js generates.",
  "generatedFrom": { "database": "ECSITE", "schema": "ANALYTICS", "object": "ORDERS", "type": "BASE TABLE" },
  "source": { "database": "ECSITE", "schema": "ANALYTICS", "object": "ORDERS" },
  "timeColumns": [{ "key": "order_date", "label": "Order Date", "column": "ORDER_DATE", "type": "DATE" }],
  "defaultTimeColumn": "order_date",
  "measures": [
    { "key": "record_count", "label": "Records", "column": "*", "agg": "COUNT", "format": "integer" },
    { "key": "amount", "label": "Amount", "column": "AMOUNT", "agg": "SUM", "format": "currency" }
  ],
  "dimensions": [
    { "key": "region", "label": "Region", "column": "REGION", "approxDistinct": 4 },
    { "key": "category", "label": "Category", "column": "CATEGORY", "approxDistinct": 12 }
  ],
  "drillPath": ["region", "category"],
  "detailColumns": ["ORDER_ID", "REGION", "CATEGORY", "AMOUNT", "ORDER_DATE"]
}
